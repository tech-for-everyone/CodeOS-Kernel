#ifndef CODEOS_VIRTIO_GPU_COMPAT_H
#define CODEOS_VIRTIO_GPU_COMPAT_H

/* Compatibility include for code that still references kernel/drivers. */
#include "../../pkgs/core/graphics/src/virtio_gpu.h"

#endif
